#include <stdio.h>

int main() {
    int i,n,next,first=0,second=1;
    printf("By using fibonacci enter your number to add-->");
    scanf("%d",&n);
    for(i=1;i<=n;i++){
        printf("%d\n",first);
    next=first+second;
    first=second;
    second=next;

        }
        
}
