#include <iostream>
#include <vector>
#include <list>
#include <stack>
#include <queue>
#include <deque>
#include <set>
#include <map>
using namespace std;

int main() {

vector<int>v;
v.push_back(10);
v.push_back(20);
v.push_back(30);
v.pop_back();
//v.empty();
//v.clear();

list<int>l;
l.push_back(10);
l.push_back(20);
l.push_front(30);
l.push_front(40);
l.pop_back();
l.pop_front();
//l.empty();
//l.clear();

stack<int>s;
s.push(10);
s.pop();
//s.top();
//s.empty();
//s.clear();

queue<int>q;
q.push(10);
q.push(20);
q.push(30);
q.push(40);


deque<int>d;
d.push_front(20);
d.push_back(30);
d.push_front(10);

set<int>se;
se.insert(50);
se.insert(20);
se.insert(30);
se.insert(20);
se.count();

map<int,string>m;
m[1]="aman";
m[2]="rahul";
m[3]="riya";

cout<<"VECTOR\n\n";
for(auto x:v)
cout<<x<<" ";

cout<<"\n\nLIST\n\n";
for(auto x:l)
cout<<x<<" ";

cout<<"\n\nSTACK\n\n";
s.push(20);
cout<<s.top();

cout<<"\n\nQUEUE\n\n";
q.pop();
cout<<q.front();

cout<<"\n\nDEQUE\n\n";
for(auto x:d)
cout<<x<<" ";

cout<<"\n\nSET\n\n";
for(auto x:se)
cout<<x<<" ";

cout<<"\n\nMAP\n\n";
for(auto x:m)
cout<<x.frist<<" "<<x.second;


    return 0;
}
